name: Build V8 + Fuzzilli → GHCR

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/v8-fuzz
  IMAGE_TAG: latest

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Free disk space before build
        run: |
          sudo apt-get clean
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          sudo rm -rf /var/lib/apt/lists/*
          rm -rf $HOME/.cache/*
          df -h

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            git curl python3 python3-pip \
            build-essential ninja-build \
            clang lld pkg-config ca-certificates

      - name: Install Swift via Swiftly
        run: |
          curl -O https://download.swift.org/swiftly/linux/swiftly-$(uname -m).tar.gz
          tar zxf swiftly-$(uname -m).tar.gz
          ./swiftly init --quiet-shell-followup
          . "${SWIFTLY_HOME_DIR:-$HOME/.local/share/swiftly}/env.sh"
          hash -r
          swift --version

      - name: Install depot_tools
        run: |
          git clone https://chromium.googlesource.com/chromium/tools/depot_tools.git
          echo "$PWD/depot_tools" >> $GITHUB_PATH

      - name: Fetch and build V8
        run: |
          fetch v8
          cd v8
          git checkout main
          gclient sync
          gn gen out/fuzzbuild --args='
            is_debug=false
            dcheck_always_on=true
            v8_static_library=true
            v8_enable_verify_heap=true
            v8_enable_partition_alloc=false
            v8_fuzzilli=true
            sanitizer_coverage_flags="trace-pc-guard"
            target_cpu="x64"
          '
          ninja -C out/fuzzbuild d8

      - name: Save d8 and delete V8 to free space
        run: |
          mkdir -p build-out
          cp v8/out/fuzzbuild/d8 build-out/
          rm -rf v8
          df -h

      - name: Cache SwiftPM build dirs
        uses: actions/cache@v4
        with:
          path: |
            fuzzilli/.build/checkouts
            fuzzilli/.build/artifacts
          key: ${{ runner.os }}-swiftpm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-swiftpm-

      - name: Clone Fuzzilli
        run: git clone https://github.com/googleprojectzero/fuzzilli.git

      - name: Build all Fuzzilli products
        run: |
          cd fuzzilli
          swift build -c release

      - name: Copy FuzzilliCli binary
        run: |
          cp fuzzilli/.build/release/FuzzilliCli build-out/Fuzzilli

      - name: Write Dockerfile
        run: |
          mkdir docker
          mv build-out/* docker/
          cat > docker/Dockerfile << 'EOF'
          FROM ubuntu:22.04
          RUN apt-get update && apt-get install -y \
              clang libstdc++6 libgcc-s1 \
              && rm -rf /var/lib/apt/lists/*
          WORKDIR /opt/fuzzing
          COPY d8 /opt/fuzzing/d8
          COPY Fuzzilli /opt/fuzzing/Fuzzilli
          RUN chmod +x d8 Fuzzilli
          ENV PATH="/opt/fuzzing:${PATH}"
          ENTRYPOINT ["./Fuzzilli"]
          EOF

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker image
        run: |
          docker build docker -t $IMAGE_NAME:$IMAGE_TAG

      - name: Push image to GHCR
        run: |
          docker push $IMAGE_NAME:$IMAGE_TAG
